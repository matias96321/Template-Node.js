    <div class="body-card">
        <table class="table table-hover">
        <thead>
            <tr>
            <th scope="col">id</th>
            <th scope="col">Nome</th>
            <th scope="col">E-mail</th>
            <th scope="col">Senha</th>
            <th scope+"col">Ação</th>
            </tr>
        </thead>
        <tbody>
            {{#each lista}}
            <tr>
            <td>{{id}}</td>
            <td>{{nome}}</td>
            <td>{{email}}</td>
            <td>{{senha}}</td>
            <td><a href="/editar/{{id}}"><button class="btn btn-info">Editar</button></a>
                <form action="/deletar" method="POST"> 
                    <input type="hidden" name="_id" value="{{id}}">
                    <a href=""><button class="btn btn-danger mt-3">Deletar</button></a></td>
                </form>
            <tr>
            {{else}}
            <h1 class="mt-4">Nenhum usuário</h1>
            {{/each}}

        </tbody>
        </table>
        <a href="/"><button class="btn btn-dark mt-5">Voltar</button></a>
    </div>
    
</div>




